let base =  parseInt(prompt("ingrese la base del rectangulo"))
let altura =  parseInt(prompt("ingrese la altura del rectangulo"))

function calcular_perimetro(b,a) {
    let resultado = 2*(a+b)  
    return resultado
}
